import React, { Fragment } from 'react';

const CurrentScore = _ => {
  return (
    <Fragment>
      <div className="cScoreHeader">
          <span>Current Score</span>
      </div>
    </Fragment>
  );
}

export default CurrentScore;
